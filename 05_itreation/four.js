const myobject={
    js:'javascrpit',
    cpp:'c++',
    swift:'apple devloper'
}
for (const key in myobject) {
    console.log(`${key} shortuct is for ${myobject[key]} `);
    
    }

const programing=["js","rubi","cpp","py","swfit"]
for (const key in programing) {
    console.log(programing [key])
    
    }

